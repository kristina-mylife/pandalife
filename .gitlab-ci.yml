pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html manifest.json sw.js public/
    - cp -r css js images icons public/
  artifacts:
    paths:
      - public
  only:
    - main
